# Weekly analytics review — 2026-06-01 (week 23)

Stop. Open both dashboards before answering. Five-minute job, do not skip.

- **Plausible**: https://plausible.io/stonemegan.dev?period=7d
- **Google Search Console**: https://search.google.com/search-console?resource_id=sc-domain:stonemegan.dev
  (filter: last 7 days)

Site state (auto-filled from repo):
- **8 published articles** — 3 dev-diary, 3 nurse-ai, 2 ece-ai
- Most recent: `when-i-do-not-reach-for-the-ai-the-cases-that-need-hands-not` (2026-06-01)
- This week's ships (2): writing-better-patient-education-handouts (May 31), when-i-do-not-reach-for-the-ai (Jun 1)
- **Content queue depth**: 2 pending entries (1 nurse-ai, 1 ece-ai) → pipeline starves in ~2 days at current cadence. Refill before Jun 3.
- **Pre-existing brand-leak slug**: `/blog/claude-nursing-handoff-notes` still contains "claude" — scheduled for slug-rewrite + redirect.

## ⚠️ Autofill blocked (Q1–Q5 need external sources)

This report was generated by `analytics-reader`, but the agent cannot autofill Q1–Q5 because:

- **No `PLAUSIBLE_API_KEY`** in `.env` → can't hit `/api/v1/stats/aggregate`
- **Plausible public dashboard at https://plausible.io/stonemegan.dev returns 404** → not set to public-shared
- **No `GSC_SERVICE_ACCOUNT`** (base64 service-account JSON) → can't hit GSC Search Analytics API
- **No Beehiiv stats API call** wired (would need `BEEHIIV_API_KEY` + publication ID)

**To unblock future weeks** (pick one, lowest-friction first):
1. **Plausible "Public dashboard"** — Plausible dashboard → Site settings → Public dashboard → enable → copy share link. Cheapest path, no API key needed.
2. **Plausible API key** — Plausible dashboard → Account → API keys → create one → put in `.env` as `PLAUSIBLE_API_KEY`. Then this report auto-fills Q1, Q2, Q4.
3. **GSC service account** — GCP console → IAM → service account → add as user on the GSC property → JSON key → base64 → `.env` as `GSC_SERVICE_ACCOUNT`. Then Q3 auto-fills.

Until then, Q1–Q5 are the user's manual job. The 5-minute version: open each dashboard, type the numbers below, commit.

---

## The 5 questions (answer in this file, commit it)

### 1. Top non-direct source this week

Plausible → Sources tab. Ignore Direct/None.

> Answer:

If still 100% Direct, the promo channels are not delivering yet —
that's the next-week priority, not a metric problem.

### 2. Top entry page

Plausible → Top Pages → Entry Pages. Note the path.

> Answer:

If `/` dominates, SEO is not landing people on long-tail articles
yet (expected for a < 30-day-old site). If a niche page or specific
article ranks #1, that's a SEO win — note which.

### 3. Top GSC query (impressions, last 7d)

GSC → Performance → Queries.

> Answer:

Note the top 3 queries by impressions. The query Google is showing
your site for is the topic to write the next piece about — even if
CTR is currently 0%.

### 4. Highest bounce + reason

Plausible → Top Pages → click the page → see bounce rate.

Page with highest bounce:
> Answer:

Hypothesis for why:

- [ ] Long load time
- [ ] First paragraph buries the lede
- [ ] No clear next-action (read next / subscribe)
- [ ] Mismatch with what the inbound source promised
- [ ] Other:

### 5. Subscribes this week

Beehiiv dashboard → Audience growth → Last 7 days.

> Count:

`PUBLIC_BEEHIIV_FORM_ID` is **set** in `.env` (verified 2026-06-01) — the embed should render. If subscribers are 0 despite traffic, the suspects are: form not visible above-the-fold on mobile (check on a phone), pitch copy weak, or no internal CTA from popular pages. Open one article in an incognito browser and check the embed renders before suspecting wiring.

---

## Action for next week

Based on the answers above, pick ONE thing to do this week:

- [ ] Refill content-queue (only 2 pending — pipeline starves ~Jun 3)
- [ ] Enable Plausible public dashboard OR add `PLAUSIBLE_API_KEY` → unblock auto-Q1/Q2/Q4
- [ ] Add `GSC_SERVICE_ACCOUNT` → unblock auto-Q3
- [ ] Write a new article targeting the top GSC query from Q3 (once Q3 is answerable)
- [ ] Drop a Reddit post in the sub that drove most traffic from Q1
- [ ] Fix the page with highest bounce from Q4
- [ ] Other:

Commit this file when answered:

```bash
git add docs/marketing/weekly-analytics/2026-06-01.md
git commit -m "analytics: week 23 review"
```
